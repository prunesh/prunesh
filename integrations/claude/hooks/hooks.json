{
  "description": "prunesh hooks: PreToolUse rewrites Bash commands to prunesh; PostToolUse filters Read and MCP",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/prunesh-pre-tool-use.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__.*|Read",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/prunesh-post-tool-use.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
